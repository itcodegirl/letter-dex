# Contributor instructions

Read `CLAUDE.md` and `PLAN.md` before making changes. `CLAUDE.md` is the
tiebreaker for conflicts. Work on one approved phase or sub-phase per pull
request, keep engine and data changes in separate pull requests, run `npm test`
before every pull request, and leave merges to Jenna.
